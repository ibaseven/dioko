import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'dioko.app.transfert',
  appName: 'Dioko',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
